{
  "name": "emergence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-based modeling playground — simple rules, complex behavior",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "tsc && node --test 'dist/test/**/*.test.js'",
    "serve": "node serve.mjs",
    "start": "npm run build && node serve.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
